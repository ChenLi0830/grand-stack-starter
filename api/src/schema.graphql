type Address {
  address: String!
  amount_received: Float
  sendTo: [Address] @relation(name: "Transaction", direction: OUT)
  receiveFrom: [Address] @relation(name: "Transaction", direction: IN)
  # actors: [Person] @relation(name: "Transaction", direction: IN)
}
